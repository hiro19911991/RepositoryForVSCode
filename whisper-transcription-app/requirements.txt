# Whisper文字起こしアプリ with Speaker Diarization
# 必須パッケージ

# Core dependencies
streamlit>=1.28.0
torch>=2.0.0
torchaudio>=2.0.0
openai-whisper>=20230918
librosa>=0.10.0
numpy>=1.24.0
soundfile>=0.12.0
ffmpeg-python>=0.2.0
pydub>=0.25.1

# Speaker Diarization (pyannote.audio)
# pyannote.audioには Hugging Face Access Token が必要です
# https://huggingface.co/pyannote/speaker-diarization-3.1 の利用規約に同意してください
pyannote.audio>=4.0.0
huggingface-hub>=0.26.0

# Optional: ReazonSpeech v2.0 (日本語音声認識 / sherpa-onnxベース・Python 3.13対応)
# pip install git+https://github.com/reazon-research/reazonspeech.git#subdirectory=pkg/k2-asr
# 依存: sherpa-onnx（自動インストール）、GPU版はsherpa-onnx-cudaが別途必要

# Optional: WhisperX (高速文字起こし + 単語レベルタイムスタンプ + 話者分離統合)
# pip install whisperx
# 依存: faster-whisper, pyannote.audio, wav2vec2アライメントモデル
# GPU推奨（CUDA対応PyTorchが必要）

# Optional: Voxtral-Mini-4B-Realtime-2602 dependencies (超低遅延音声認識)
# GPUメモリ16GB以上が推奨されます
# vllm>=0.17.0
# mistral-common>=1.9.0
# transformers>=4.45.0
